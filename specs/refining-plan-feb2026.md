So I want to continue. It was some gap in time, that's why I need to catch up as well. So you know, I want to build this Bayesian LLM, Bayesian LLM, let's say. I want to start from scratch and I'm not sure that this structure, this folder is correct, that's why I need to realign, refine the plan, because this structure probably has been created by another AI assistant. Never mind. Let's refine our plan. I'm going to review a few papers. I will share with you like four papers, but probably these papers, they are biased to something like LoRA and something like that. So what I have in mind, you know, I want to make it very simple, sort of. I want to start from like MiniGPT. Now it's super clear and easy. I even have some code. I did it already, but probably you can do it on your side. So what does it mean, MiniGPT? Right now, it's possible to create, like, let's say, GPT-2 or 3 very easy while just using kind of small numbers, kind of number of layers, number of transformer blocks, small number, lengths on some dimensionality lengths, number of heads, and so on and so forth. Everything can be much smaller while architecture will be completely the same, maybe without mixture of expert. That's why they probably use... Right now, and I'm talking like GPT, like some abstractions. So basically, there are lots of things like, for example, deep-sea and other things with good things like for positioning and for order position and lots of lots of things, but we are not going to repeat everything. It must be very, very simple and like basic things because I want to focus on Bayesian. Okay, let's say we have this model, right? The question, we need to train it, right? So I'm pretty sure that right now it's... we should not reinvent the wheel. So it's very easy to find the best dataset that usually people use, like for hello, like Hello World program for creating this MiniGPT. You need to research it, you need to provide. I don't want to spend my time on this boilerplate information. So, okay, we will train it. Yeah, let's use PyTorch. I see that there is in the documentation of this repository, I see Python.distribution. If PyTorch has this distribution sublibrary or models, it's completely okay. I guess that they quite close to TensorFlow distribution, which is I completely in big love. I really like TensorFlow distribution. That's why we can use all this stuff. And you know, I built Bayesian neural nets with... A posteriori and a priori with some layers, Bayesian layers, so called. I used, yeah, I used TensorFlow distribution. If it's possible to do with PyTorch distribution, it's very good. Let's not to think about JAX right now, we can do it later. As for the distribution, PyTorch distribution, even if some things will require some manual work, it's not a big problem. It's better to use PyTorch than TensorFlow anyway. And yeah, it's good not to use any additional libraries for this Bayesian things. So what else? So definitely we need to train this mini GPT, right? Otherwise, it makes no sense just calling architecture. It's a bad idea. So we need to train, evaluate it in a simple way, but we need to create these quick models for evaluation, right? Because we need to have holdout set for validation, holdout set for the test. So we need to have everything basically to build this process, to make it very smooth, not to repeat things, for example, if it's evaluation model, we can run it quickly and so on. So another thing, we will avoid using any notebooks here. So no notebooks and please bake it as a rule in your memory, agents.md. No. No, notebooks, just code that can be run. And again, please revise this folder. I see this SRC, Bayesian LLM, lots of folders I don't like. Maybe I will, I would ask you to just remove this weird stuff, or I will do it on my side. It seems like another AI assistant created these weird things. I don't like this. I want to start from scratch and move steadily, kind of, creating what we really want. Okay, let's say that we have this MiniGPT. So, the next step probably will be exactly attempt to add some layer of Bayesian layers. And, you know, and it will be a question, what layers should be augmented with this or substitute replaces with Bayesian layers, let's say. Is it projection to key value queries, or is it projection for feedforward LMs, or both? So, it's interesting. I'm really curious about your thoughts about this. This stuff must be for sure proven by some papers and so on. But I guess you understand what I mean. What layers of transformers should be replaced with Bayesian layers? I need to kind of your informed choice. Okay. And after that, this MiniGPT, like second version MiniGPT with Bayesian layers, must be also evaluated, right? We need to train it, we need to maybe even fine-tune it just, I don't know. And we need to evaluate it in our dataset, great, but what else? I want to, from the very beginning, we need to create a framework for kind of evaluation, evaluating exact Bayesian aspects of Bayesian LLMs, because, you know, okay, let's say we trained this Bayesian LLM, even this mini GPT very basic. From the very beginning, I want to build the tool that somehow measure uncertainty on Bayesian machine learning model. And here again, I need your help. I need your best choice, approved, informed choice, I mean, approved with, approved by these papers. So what do I mean? How to, the question here is, how to measure this uncertainty of, I mean, epistemic uncertainty. So I guess that let's say I have a specific dataset, some test held out or specific held out for uncertainty measurement, right? So if I have this test test set, what I can do, I can use this, like, say, like, prompts, right? Every row on prompt and to generate some text, but the question here, what I will measure. So every weight of Bayesian layer is kind of stochastic, right? Let's say it's normal. That's why it will have at least a mean and variance, but probably it will be covariance matrix, but we will see, maybe we will simplify it and kind of make them independent. It's another story we will talk about later. But anyway, this is just This is just parameters of, uh, parameters of weights. Okay, but how to measure this epistemic uncertainty for one run, for one sample, let's say? I, my guess, but I need your help, that what we can measure, we can measure, we can run a few times and we should look at sort of distribution of variants of logits, something like this, of logits of final layers, but please let me know, please help me to, to work on this. I mean, since it's Bayesian, Bayesian neural nets and neural network, first of all, yeah, we will use, we will, we shouldn't use any sampling, right, uh for this LLM, so temperatures should be zero, but to, we shouldn't even inference probably. Uh, yeah, we will inference. Maybe we can even sample, but probably I will, I would use a temperature zero. But the matter here that for every token, token, every logit can be, can be measured, can be kind of saved, and we need to run one prompt, same prompt a few times, let's say up to 10 times, and to see, uh, to which extent and to see kind of distribution of variants of these logits for, for every prompt, but again. We need some magic, so what is logits? Vocabulary is very high. That's why we should use the highest logit, probably, with the highest number. Maybe per every run, we can take top three tokens with the highest logits of highest values. I hope you see what I mean. So, yeah, it's really important how we build this epistemic uncertainty. Or please provide another approach, because it's important from the very beginning. The idea of Bayesian LLM is to provide some tool to not only generate text, but augment final generation with some confidence. In my case, it's epistemic confidence, not aleatoric. So it's about how, to which extent the model is confident about the information that is in prompt, because it could be trained on the data, kind of other data, and on test time, this model, let's say, use data that it didn't train on. And this is important also to put it in the framework of evaluation, because, you know, from the very beginning, I need to see that I train this model on these aspects, these categories, topics, let's say, and I want to have very specific topics that I'm pretty sure that model kind of hasn't seen or like hasn't been trained on. And I want to measure the epistemic uncertainty on these topics, and I should clearly see that this epistemic uncertainty, it's higher than for topics that... that the model was trained, so you see, we need to build this framework from the very beginning. I mean, yeah, building first a mini-GPT, primitive things without modern, even modern kind of improvements like Swiss glue, like RoPair, rotational position encoding, and lots of lots of things like sliding window attention, lots of things we should not do it because these aspects we can apply later. So mini-GPT, good architecture with small numbers. First step, adding Bayesian layers, replacing some layers, we need to make this decision. And after that, immediately what we need to do immediately is framework of measuring with this epistemic uncertainty and having some process of comparing this epistemic uncertainty for topics that model, this model has been trained on and with comparing with the epistemic uncertainty in topics that this model has not been trained on, and so on and so forth. So this is the first one of a few stages, basically first big stage. Later we will think what to do. Later it's another story. We can improve the architecture, we can think about kind of extending Bayesian replacement and so on and so forth. And at some stage later, I would think about Laura adapters, but it's absolutely another research. If you, if you understand what I mean for Laura adapters research, I would, I would use even a kind of modern big existing open-weight LLM. So it's an absolutely another story. It's not miniGPT. I would take existing trained LLM, and I will try to train my Laura adapters. We're using some specific techniques of kind of, I mean, Bayesian Laura adapters. So these Laura adapters will have Bayesian layers probably only in one part of this adapter. But again, it's another story. We will, we can talk about this later. So your task now is to think a lot, take your time and return back to me with all your thoughts around what I said, and we need to build very clear plan of the next steps.